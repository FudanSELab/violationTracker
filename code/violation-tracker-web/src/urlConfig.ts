/** new
 * ip地址变动时，注意要更改以下2个地址:
 * 1) baseIp
 * 2) homepage (注意端口号)
 */
export const baseIp = 'http://127.0.0.1';
export const homepage = `${baseIp}:8888/`;
export const baseUrl = `${baseIp}:8000/`;
export const projectRoot = '';
